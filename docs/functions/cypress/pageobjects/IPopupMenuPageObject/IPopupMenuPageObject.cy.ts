import { IPopupMenuPageObject } from "@fkui/vue/cypress";
import Example from "./IPopupMenuPageObject.vue";

it("should [...]", () => {
    cy.mount(Example);

    /* --- cut above --- */
    /* eslint-disable-next-line @typescript-eslint/no-unused-vars -- placeholder code */
    const obj = new IPopupMenuPageObject("[data-test=awesome-component]");
    /* --- cut below --- */

    throw new Error("Not implemented");
});
