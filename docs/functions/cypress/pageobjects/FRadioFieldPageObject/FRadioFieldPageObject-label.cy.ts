import { FRadioFieldPageObject } from "@fkui/vue/cypress";
import Example from "./FRadioFieldPageObject-label.vue";

it("should [...]", () => {
    cy.mount(Example);

    /* --- cut above --- */
    /* eslint-disable-next-line @typescript-eslint/no-unused-vars -- placeholder code */
    const obj = new FRadioFieldPageObject("[data-test=awesome-component]", "");
    /* --- cut below --- */

    throw new Error("Not implemented");
});
