import { FTableColumnPageObject } from "@fkui/vue/cypress";
import Example from "./FTableColumnPageObject-tableRowHeaderContent.vue";

it("should [...]", () => {
    cy.mount(Example);

    /* --- cut above --- */
    /* eslint-disable-next-line @typescript-eslint/no-unused-vars -- placeholder code */
    const obj = new FTableColumnPageObject("[data-test=awesome-component]", "");
    /* --- cut below --- */

    throw new Error("Not implemented");
});
