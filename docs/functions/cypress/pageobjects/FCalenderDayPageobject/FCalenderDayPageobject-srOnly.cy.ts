import { FCalenderDayPageobject } from "@fkui/vue/cypress";
import Example from "./FCalenderDayPageobject-srOnly.vue";

it("should [...]", () => {
    cy.mount(Example);

    /* --- cut above --- */
    /* eslint-disable-next-line @typescript-eslint/no-unused-vars -- placeholder code */
    const obj = new FCalenderDayPageobject("[data-test=awesome-component]");
    /* --- cut below --- */

    throw new Error("Not implemented");
});
