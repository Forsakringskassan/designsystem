import { FOutputFieldPageobject } from "@fkui/vue/cypress";
import Example from "./FOutputFieldPageobject-body.vue";

it("should [...]", () => {
    cy.mount(Example);

    /* --- cut above --- */
    /* eslint-disable-next-line @typescript-eslint/no-unused-vars -- placeholder code */
    const obj = new FOutputFieldPageobject("[data-test=awesome-component]");
    /* --- cut below --- */

    throw new Error("Not implemented");
});
