import { AlertScreenReaderPageObject } from "@fkui/vue/cypress";
import Example from "./AlertScreenReaderPageObject.vue";

it("should [...]", () => {
    cy.mount(Example);

    /* --- cut above --- */
    /* eslint-disable-next-line @typescript-eslint/no-unused-vars -- placeholder code */
    const obj = new AlertScreenReaderPageObject();
    /* --- cut below --- */

    throw new Error("Not implemented");
});
