import { FCheckboxGroupPageObject } from "@fkui/vue/cypress";
import Example from "./FCheckboxGroupPageObject-content.vue";

it("should [...]", () => {
    cy.mount(Example);

    /* --- cut above --- */
    /* eslint-disable-next-line @typescript-eslint/no-unused-vars -- placeholder code */
    const obj = new FCheckboxGroupPageObject("[data-test=awesome-component]");
    /* --- cut below --- */

    throw new Error("Not implemented");
});
