import { FPageHeaderPageobject } from "@fkui/vue/cypress";
import Example from "./FPageHeaderPageobject-skipLink.vue";

it("should [...]", () => {
    cy.mount(Example);

    /* --- cut above --- */
    /* eslint-disable-next-line @typescript-eslint/no-unused-vars -- placeholder code */
    const obj = new FPageHeaderPageobject("[data-test=awesome-component]");
    /* --- cut below --- */

    throw new Error("Not implemented");
});
