const { defineMetadata } = require("html-validate");

module.exports = defineMetadata({
    "x-sort-filter-dataset-ng": {
        flow: true,
    },
    "x-time-text-field": {
        flow: true,
    },
});
