/**
 * Key used on HTMLElement to store component state.
 *
 * @internal
 */
export const componentStateSymbol = Symbol("validation:component-state");

/**
 * Key used on HTMLFormElement to store form state.
 *
 * @internal
 */
export const formStateSymbol = Symbol("validation:form-state");
