export interface IconDefinition {
    /**
     * Icon name with prefix.
     */
    key: string;

    /**
     * Icon name without prefix.
     */
    name: string;

    /**
     * Human-readable name.
     */
    prettyName: string;
}
