/**
 * Predefined reasons the modal was closed.
 *
 * @public
 */
export enum ModalReason {
    /**
     * Positive response from modal.
     */
    CONFIRM = "confirm",
    /**
     * Negative response from modal.
     */
    DISMISS = "dismiss",
}
