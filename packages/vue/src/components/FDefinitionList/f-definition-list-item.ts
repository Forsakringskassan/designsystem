/**
 * Represents a definition for usage with FDefinitionList.
 *
 * @public
 */
export interface FDefinitionListItem {
    /** Definition term shown on the left side of the list. */
    term: string;

    /** Definition value shown on the right side of the list. */
    definition: string;
}
