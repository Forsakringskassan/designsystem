/**
 * Matches a single whitespace globally.
 *
 * @public
 * @deprecated Use {@link stripWhitespace} instead
 */
export const WHITESPACE_PATTERN = /\s/g;

/**
 * @public
 * @deprecated Use {@link formatNumber} instead.
 */
export const FORMAT_3_DIGITS_GROUPS = /\B(?=(\d{3})+(?!\d))/g;
