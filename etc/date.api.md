## API Report File for "@fkui/date"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

// @public
export enum DateFormat {
    FULL = "full",
    ISO8601 = "iso-8601",
    LONG = "long",
    YYYYMMDD = "YYYYMMDD"
}

// @public
export class FDate implements IterableDate<FDate> {
    addDays(value: number): FDate;
    addMonths(value: number): FDate;
    addYears(value: number): FDate;
    static compare(a: FDate | string, b: FDate | string): number;
    get day(): number;
    get dayName(): string;
    get dayNameShort(): string;
    endOfMonth(): FDate;
    equals(rhs: FDate | string): boolean;
    static fromDate(value: Date): FDate;
    static fromIso(value: string): FDate;
    static fromYearMonthDay(year: FYear | number | string, month: number | string, day: number | string): FDate;
    isAfter(rhs: FDate | string): boolean;
    isBefore(rhs: FDate | string): boolean;
    isValid(): boolean;
    get month(): number;
    get monthName(): string;
    get monthNameShort(): string;
    // (undocumented)
    next(): FDate;
    static now(): FDate;
    previous(): FDate;
    startOfMonth(): FDate;
    toJSON(): string;
    toString(format?: DateFormat): string;
    get week(): number;
    get weekDay(): Weekday | 0;
    get year(): number;
}

// @public
export class FYear implements IterableDate<FYear> {
    addYears(amount: number): FYear;
    static compare(a: FYear | number | string, b: FYear | number | string): number;
    equals(rhs: FYear | number | string): boolean;
    static fromDate(value: FDate | Date): FYear;
    static fromYear(value: number | string): FYear;
    isAfter(rhs: FYear | number | string): boolean;
    isBefore(rhs: FYear | number | string): boolean;
    isValid(): boolean;
    next(): FYear;
    static now(): FYear;
    previous(): FYear;
    toJSON(): number | null;
    toString(): string;
    get value(): number;
}

// @public
export function getLocale(): Locale;

// @public
export function getWeekdayNamings(locale?: Locale): WeekdayNaming[];

// @public
export function groupByWeek(begin: FDate, end: FDate): Array<{
    week: number;
    days: FDate[];
}>;

// @public
export function isLocale(value?: Locale | string): value is Locale;

// @public
export interface IterableDate<T> {
    equals(rhs: T): boolean;
    isBefore(rhs: T): boolean;
    next(): T;
}

// @public
export enum Locale {
    // (undocumented)
    ENGLISH = "en",
    // (undocumented)
    SWEDISH = "sv"
}

// @public
export function range<T extends IterableDate<T>>(begin: T, end: T): Generator<T>;

// @public
export function resetLocale(): void;

// @public
export function setLocale(locale: Locale): void;

// @public
export enum Weekday {
    // (undocumented)
    FRIDAY = 5,
    // (undocumented)
    MONDAY = 1,
    // (undocumented)
    SATURDAY = 6,
    // (undocumented)
    SUNDAY = 7,
    // (undocumented)
    THURSDAY = 4,
    // (undocumented)
    TUESDAY = 2,
    // (undocumented)
    WEDNESDAY = 3
}

// @public
export interface WeekdayNaming {
    // (undocumented)
    name: string;
    // (undocumented)
    shortName: string;
    // (undocumented)
    weekday: Weekday;
}

// (No @packageDocumentation comment for this package)

```
